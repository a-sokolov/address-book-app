const initialState = {
    application: {
        version: '03.000.00-rc5',
        build: '755'
    }
};

const contextReducer = (state = initialState, action) => {
    return state;
};

export default contextReducer;